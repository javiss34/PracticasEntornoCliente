"use strict";

export const generarArray = (num) => {
    let array = [];
    for (let i = 0; i < num; i++) {
        array[i]=Math.floor(Math.random()*101);   
    }
    return array;
}

const sumaInversaArrays = (array1,array2) => {
    
}

const imprimirArray = (array) => {
    console.log("Array:", )
}